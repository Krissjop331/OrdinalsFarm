server {
    listen 443;
    server_name ordinals-farm.com;

    proxy_buffers 8 16k;
    proxy_buffer_size 32k;

    location / {
        proxy_pass http://89.35.124.123:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        proxy_read_timeout 1000;
        proxy_connect_timeout 1000;
        proxy_send_timeout 1000;

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /_next/static {
        proxy_pass http://89.35.124.123:3000;
        proxy_cache_valid 60m;
        proxy_set_header Host $host;
        add_header Cache-Control "public, max-age=3600";
    }

    location /static {
        proxy_pass http://89.35.124.123:3000;
        proxy_cache_valid 60m;
        proxy_set_header Host $host;
        add_header Cache-Control "public, max-age=3600";
    }
}
    # location = /robots.txt {
    #     alias /home/ubuntu/kazefrofos2/client/robots.txt;
    #     access_log off;
    #     log_not_found off;
    # }
}

# worker_processes auto;

# events {
#     worker_connections 1024;
# }

# http {
#     include /etc/nginx/mime.types;
#     sendfile on;

#     server {
#         listen 80;
#         server_name ordinals-farm.com;
#         return 301 https://$host$request_uri;  # Перенаправляем HTTP на HTTPS
#     }

#     server {
#         listen 443 ssl;
#         server_name ordinals-farm.com;

#         proxy_buffers 8 16k;
#         proxy_buffer_size 32k;

#         location / {
#             proxy_pass http://nextjs:3000;
#             proxy_http_version 1.1;
#             proxy_set_header Upgrade $http_upgrade;
#             proxy_set_header Connection 'upgrade';
#             proxy_set_header Host $host;
#             proxy_cache_bypass $http_upgrade;

#             proxy_read_timeout 1000;
#             proxy_connect_timeout 1000;
#             proxy_send_timeout 1000;

#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#         }

#         location /_next/static {
#             proxy_pass http://nextjs:3000;
#             proxy_cache_valid 60m;
#             proxy_set_header Host $host;
#             add_header Cache-Control "public, max-age=3600";
#         }

#         location /static {
#             proxy_pass http://nextjs:3000;
#             proxy_cache_valid 60m;
#             proxy_set_header Host $host;
#             add_header Cache-Control "public, max-age=3600";
#         }
#     }
# }


server {
    listen 80;
    server_name ordinals-farm.com;

    location / {
        proxy_pass http://localhost:3000;  # Проксируем на Next.js
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        proxy_read_timeout 1000;
        proxy_connect_timeout 1000;
        proxy_send_timeout 1000;

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /_next/static {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 60m;
        proxy_set_header Host $host;
        add_header Cache-Control "public, max-age=3600";
    }

    location /static {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 60m;
        proxy_set_header Host $host;
        add_header Cache-Control "public, max-age=3600";
    }
}

server {
    listen 443;
    server_name ordinals-farm.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        proxy_read_timeout 1000;
        proxy_connect_timeout 1000;
        proxy_send_timeout 1000;

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /_next/static {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 60m;
        proxy_set_header Host $host;
        add_header Cache-Control "public, max-age=3600";
    }

    location /static {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 60m;
        proxy_set_header Host $host;
        add_header Cache-Control "public, max-age=3600";
    }
}